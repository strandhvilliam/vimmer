{
  "name": "@vimmer/generate-participant-zip",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --target node --minify --outdir dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.x",
    "@aws-sdk/s3-request-presigner": "^3.x",
    "@vimmer/supabase": "workspace:*",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vimmer/typescript-config": "workspace:*",
    "@types/node": "^20.x"
  },
  "peerDependencies": {
    "typescript": "^5.7.2"
  }
}
